{
  "name": "sanctuary-web",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "Sanctuary — a private two-person sacred space (React web SPA)",
  "scripts": {
    "prestart": "node scripts/prepare-dev-index.mjs",
    "start": "vite",
    "predev": "node scripts/prepare-dev-index.mjs",
    "dev": "vite",
    "prebuild": "node scripts/prepare-dev-index.mjs",
    "build": "tsc --noEmit && vite build",
    "build:pages": "node scripts/prepare-dev-index.mjs && tsc --noEmit && VITE_BASE=/Orcal/ vite build && node scripts/publish-pages.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "framer-motion": "^11.15.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.6"
  },
  "engines": {
    "node": ">=18"
  }
}
